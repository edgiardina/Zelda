using System;
using System.Collections.Generic;
using System.Text;

namespace Test_Game.World.ScreenTiles
{
    /*
    class BlueDungeonTile : ScreenTile
    {
        public BlueDungeonTile(Microsoft.Xna.Framework.Game game, int TileNum)
            : base(game, TileNum)
        {
            TileNumber = TileNum;
            //Layout the tiles according to what number tile this is.
            Position.X = ((TileNumber % 16 ) * 32);
            //the plus 3 here moves the map below the status bar at the top
            Position.Y = (((TileNumber / 16 ) + 3) * 32);
            // TODO: Construct any child components here


        }
    }
     */
}
